import { StreamReader } from "./stream.js";
import * as cliV2 from "@bytecodealliance/preview2-shim/cli";
export declare const _appendEnv: (env: Record<string, string>) => void, _setEnv: typeof cliV2._setEnv, _setArgs: typeof cliV2._setArgs, _setCwd: typeof cliV2._setCwd, _setTerminalStdin: (terminalStdin: unknown) => void, _setTerminalStdout: (terminalStdout: unknown) => void, _setTerminalStderr: (terminalStderr: unknown) => void, exit: typeof import("@bytecodealliance/preview2-shim/interfaces/wasi-cli-exit"), terminalInput: typeof import("@bytecodealliance/preview2-shim/interfaces/wasi-cli-terminal-input"), terminalOutput: typeof import("@bytecodealliance/preview2-shim/interfaces/wasi-cli-terminal-output"), terminalStdin: typeof import("@bytecodealliance/preview2-shim/interfaces/wasi-cli-terminal-stdin"), terminalStdout: typeof import("@bytecodealliance/preview2-shim/interfaces/wasi-cli-terminal-stdout"), terminalStderr: typeof import("@bytecodealliance/preview2-shim/interfaces/wasi-cli-terminal-stderr");
export declare const environment: {
    getEnvironment: typeof import("@bytecodealliance/preview2-shim/interfaces/wasi-cli-environment").getEnvironment;
    getArguments: typeof import("@bytecodealliance/preview2-shim/interfaces/wasi-cli-environment").getArguments;
    getInitialCwd: typeof import("@bytecodealliance/preview2-shim/interfaces/wasi-cli-environment").initialCwd;
};
export declare const stdin: {
    /**
     * Return a stream for reading from stdin, and a future to signal read results.
     *
     * WIT:
     * ```
     * read-via-stream: func() -> tuple<stream<u8>, future<result<_, error-code>>>;
     * ```
     *
     * @returns {[StreamReader, FutureReader]} A tuple of [stream, future].
     */
    readViaStream(): (StreamReader | import("./future.js").FutureReader)[];
};
export declare const stdout: {
    /**
     * Write the given stream to stdout.
     *
     * WIT:
     * ```
     * write-via-stream: func(data: stream<u8>) -> future<result<_, error-code>>;
     * ```
     * @returns {Promise<{tag: string, val?: string}>} Result of the write operation.
     */
    writeViaStream(streamReader: any): Promise<{
        tag: string;
        val: string;
    }>;
};
export declare const stderr: {
    /**
     * Write the given stream to stderr.
     *
     * WIT:
     * ```
     * write-via-stream: func(data: stream<u8>) -> future<result<_, error-code>>;
     * ```
     * @returns {Promise<{tag: string, val?: string}>} Result of the write operation.
     */
    writeViaStream(streamReader: any): Promise<{
        tag: string;
        val: string;
    }>;
};
